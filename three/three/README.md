# 方法一：  
使用memcyp，将f1与f2直接切入字符数组buf中，之后再利用memcyp将buf切出，读取到g1与g2中  
相对而言，是比较简单快速的方法
# 方法二：  
使用union，定义数组f及为将float录入字符数组b，后面将union pack.读取到a1，a2中即可得到对应浮点数  
相对而言，是最快的且占用空间最小的办法
# 方法三：  
直接手动利用指针切入切出，得到float时还要解除指针  
相对而言最麻烦，操作最多的方法。